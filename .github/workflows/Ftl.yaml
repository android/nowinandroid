name: FTL build

on:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ftl-run:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Validate Gradle Wrapper
        uses: gradle/wrapper-validation-action@v1

      - name: Copy CI gradle.properties
        run: mkdir -p ~/.gradle ; cp .github/ci-gradle.properties ~/.gradle/gradle.properties

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: 17

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2

      - name: Get service account JSON file
        env:
          PLAY_FILE: ${{ secrets.SERVICE_ACCOUNT }}
        run: |
          echo $SERVICE_ACCOUNT  > ./app/service-account.json

      - name: Run Android tests on FTL managed device
        run: ./gradlew app:myFtlDeviceDemoDebugAndroidTest
